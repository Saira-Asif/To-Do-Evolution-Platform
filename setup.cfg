[tool:pytest]
testpaths = tests
python_files = test_*.py
addopts =
    --cov=src
    --cov-report=html
    --cov-report=term-missing
    --cov-fail-under=90

[tool.coverage.run]
source = src/
omit =
    */venv/*
    */__pycache__/*
    */migrations/*